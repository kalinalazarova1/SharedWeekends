@model Byte[]

@if (Model != null)
{
    var photoString = "data:image/png;base64," + Convert.ToBase64String(Model);
    <img src="@photoString" class="img-thumbnail" alt="Avatar" />
}
else
{
    <img src="~/Content/default-avatar.jpg" class="img-thumbnail" alt="Avatar" />
}
